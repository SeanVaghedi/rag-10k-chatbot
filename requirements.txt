# Core app
streamlit

# LangChain framework + provider integrations
langchain>=0.1.0
langchain-community
langchain-google-genai
langchain-openai
langchain-anthropic
langchain-ollama

# Provider SDKs
google-generativeai
openai
anthropic

# Document loading / parsing
pypdf

# Vector store + tokenization
faiss-cpu
tiktoken

# Config / environment
python-dotenv

# Web API layer (FastAPI + uvicorn) — required to serve backend.main:app.
# Chained here so a single `pip install -r requirements.txt` (and Railway's
# Nixpacks build, which installs only this file) covers the full backend.
-r backend/requirements.txt
