# Base URL of the FastAPI backend.
# Defaults to http://localhost:8000 when unset.
# For a deployed backend, set this to its public URL (and set FRONTEND_ORIGIN
# on the backend to this app's URL so CORS allows it).
NEXT_PUBLIC_API_URL=http://localhost:8000
